<div class="container">
  <span><%= gravatar_for @user %></span>
  <h1><%= @user.email %></h1>
</div>

<br>
<%= render partial: 'posts/post_form' if @user == current_user %>
<%= render partial: 'feed' %>